# Refer for explanation to following link:
# https://github.com/evilmartians/lefthook/blob/master/docs/full_guide.md

pre-commit:
  parallel: true
  commands:
    build:
      run: nix build .?rev=$(git rev-parse HEAD)#firmware --no-link -Lv
